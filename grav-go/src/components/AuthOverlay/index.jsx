// AuthOverlay removed: Use `AuthPages`, `LoginForm` and `RegisterForm` which interact with the backend.
// This file is kept as a no-op placeholder to avoid breaking any imports.
import React from 'react';

export default function AuthOverlay(){
  return null;
}
